# ============================================================
# VPS DevOps Agent — Configuration d'environnement
# ============================================================
# IMPORTANT: Copiez ce fichier en .env et remplissez TOUTES
# les valeurs obligatoires avant de démarrer l'application.
# NE commitez JAMAIS le fichier .env dans Git.
# ============================================================

# --- Serveur ---
PORT=4000
NODE_ENV=production

# --- Sécurité (OBLIGATOIRE) ---
# [REQUIRED] Minimum 32 caractères aléatoires.
# Générez-en un avec : node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Le serveur REFUSE de démarrer si cette valeur est absente ou trop courte.
JWT_SECRET=REMPLACEZ_PAR_UNE_CHAINE_ALEATOIRE_DE_MIN_32_CARACTERES

# [REQUIRED] Identifiant admin pour le premier démarrage.
ADMIN_USERNAME=admin

# [REQUIRED] Minimum 8 caractères. Le serveur refuse de démarrer sans ce paramètre.
# Ne jamais utiliser 'admin2025', 'Admin2024' ou toute valeur par défaut connue.
ADMIN_PASSWORD=REMPLACEZ_PAR_VOTRE_MOT_DE_PASSE_SECURISE

# --- API IA (choisir un provider) ---
OPENAI_API_KEY=sk-...
# DEEPSEEK_API_KEY=sk-...
AI_PROVIDER=openai

# --- Configuration Agent ---
AGENT_WORKSPACE=/opt/agent-projects
ALLOWED_DOCKER_NETWORKS=agent-network
MAX_CONCURRENT_TASKS=3

# --- Sécurité avancée ---
ENABLE_DRY_RUN=true
REQUIRE_APPROVAL=true
ALLOWED_COMMANDS=docker,npm,node,git,mkdir,chmod

# --- Alertes Email (optionnel) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# --- Alertes Telegram (optionnel) ---
# TELEGRAM_BOT_TOKEN=your-bot-token
# TELEGRAM_CHAT_ID=your-chat-id

# --- CORS (optionnel, recommandé en production) ---
# ALLOWED_ORIGINS=https://votre-domaine.com,https://dashboard.votre-domaine.com

# --- Paiements (optionnel) ---
# STRIPE_PUBLIC_KEY=pk_live_...
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...
